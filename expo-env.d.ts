/// <reference types="expo/types" />

// Committed intentionally so `tsc` (locally and in CI) has Expo's ambient types
// — including EXPO_PUBLIC_* env typing — without needing `expo start` to have run.
