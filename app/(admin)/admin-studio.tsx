import { StudioScreen } from '../../src/studio/StudioScreen';

/** The admin's Studio tab — the same shared screen every painting role gets. */
export default function AdminStudio() {
  return <StudioScreen />;
}
